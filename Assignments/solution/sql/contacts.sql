CREATE TABLE contacts (
    id INT AUTO_INCREMENT PRIMARY KEY,
    fname VARCHAR(50),
    lname VARCHAR(50),
    address VARCHAR(100),
    city VARCHAR(50),
    state VARCHAR(10),
    phone VARCHAR(20),
    email VARCHAR(100),
    dob VARCHAR(20),
    contacts VARCHAR(200),
    age VARCHAR(20)
);
